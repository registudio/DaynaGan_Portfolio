---
title: ROSA × ROS 2
slug: rosa-ros2
order: 1
year: 2024
status: complete
summary: A LangChain-based ROSA agent that turns plain-English instructions into ROS 2 commands for robots simulated in NVIDIA Isaac Sim.
technologies: [ROS 2, LangChain, ROSA, NVIDIA Isaac Sim, Python]
accent: '#b794f6'
compare:
  caption: Illustrative example of the interface change.
  before:
    label: Before
    code: |
      $ ros2 topic pub --once /cmd_vel \
          geometry_msgs/msg/Twist \
          "{linear: {x: 0.5}, angular: {z: 0.0}}"
  after:
    label: After
    code: |
      > Drive forward half a metre,
        then tell me what the lidar sees.
parts:
  - id: brain
    label: ROSA agent core
    shape: sphere
    size: [0.26]
    position: [0, 0.78, 0]
    explode: [0, 1.25, 0]
    material: glow
    summary: The LLM agent that interprets natural-language instructions and plans which ROS 2 tools to call.
    did: Implemented a ROSA agent using LangChain to support natural-language control of ROS 2 robots in Isaac Sim.
  - id: tools
    label: Tool layer
    shape: torus
    size: [0.42, 0.035]
    position: [0, 0.78, 0]
    rotation: [90, 0, 0]
    explode: [0, 0.85, 0]
    material: chrome
    summary: LangChain tools that wrap ROS 2 topics, services and parameters so the agent can act on the robot.
  - id: compute
    label: ROS 2 compute deck
    shape: box
    size: [1.0, 0.18, 0.7]
    position: [0, 0.3, 0]
    explode: [0, 0.45, 0]
    material: violet
    summary: The ROS 2 node graph that carries commands from the agent down to the robot.
  - id: lidar
    label: Lidar
    shape: cylinder
    size: [0.13, 0.15, 0.16]
    position: [0.42, 0.48, 0]
    explode: [0.9, 0.7, 0]
    material: chrome
    summary: Perception sensor the agent can query to describe its surroundings.
  - id: chassis
    label: Simulated chassis
    shape: box
    size: [1.4, 0.32, 1.0]
    position: [0, 0, 0]
    explode: [0, -0.55, 0]
    material: graphite
    summary: The robot body, simulated in NVIDIA Isaac Sim.
    did: Evaluated NVIDIA Isaac Sim with ROS 2 as a possible primary simulation platform for future robotics projects.
  - id: wheels
    label: Drive wheels
    shape: cylinder
    size: [0.22, 0.22, 0.12]
    rotation: [90, 0, 0]
    position: [0.48, -0.12, 0.56]
    explode: [0.25, -0.35, 0.55]
    material: lilac
    copies:
      - { position: [-0.48, -0.12, 0.56], explode: [-0.25, -0.35, 0.55] }
      - { position: [0.48, -0.12, -0.56], explode: [0.25, -0.35, -0.55] }
      - { position: [-0.48, -0.12, -0.56], explode: [-0.25, -0.35, -0.55] }
    summary: Differential drive, receiving velocity commands published by the agent.
---
## Overview

Built during my robotics internship at A*STAR I²R. ROSA (the Robot Operating System Agent) lets an operator control a ROS 2 robot in plain English instead of memorising topic names and message formats.

## What I did

- Implemented a ROSA agent using LangChain to support natural-language control of ROS 2 robots in Isaac Sim.
- Evaluated NVIDIA Isaac Sim with ROS 2 as a possible primary simulation platform for future robotics projects.
